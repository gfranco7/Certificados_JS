# Backend - API Certificados (Mock)

- Instalar deps:
```
npm install
```

- Desarrollo:
```
npm run dev
```

- Endpoints:
- `GET /api/health`
- `GET /api/certificates?cedula=XXXXXXXX`
- `GET /api/_all` (solo pruebas)

# Frontend - Verificador de Certificados

- Ejecutar en desarrollo:
```
npm install
npm run dev
```

- Proxy a backend: `/api` → `http://localhost:4000` (configurado en `vite.config.js`).

- Componente reusable: `src/components/CertificateLookup.jsx`
